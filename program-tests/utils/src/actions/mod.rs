/// Legacy actions module - contains actions moved from light-token-client
/// for backwards compatibility during the refactoring process.
pub mod legacy;

// Re-export legacy module contents for backward compatibility
pub use legacy::*;
