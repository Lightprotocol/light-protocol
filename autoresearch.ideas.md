- Vendor `pinocchio-token-program` (and possibly `pinocchio-token-interface`) into the repo and migrate that dependency to Pinocchio 0.10. Current git dependency is pinned to `pinocchio = 0.9`, which likely explains the remaining `AccountInfo`/`ProgramError` type mismatches in `light-compressed-token`.
- If vendoring stalls, replace the last `pinocchio-token-program` processor call sites inside `programs/compressed-token/program/src/ctoken/*` with local zero-copy token logic; the current blockers are concentrated in `transfer`, `approve/revoke`, and `burn*` expecting old `pinocchio::account_info::AccountInfo` APIs.
