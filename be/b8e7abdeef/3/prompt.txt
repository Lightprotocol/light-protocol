Implement the following plan:

# Gate Network-Fee Tree Creation to Protocol Authority

**Date:** 2026-03-02

## Context

Currently, anyone can create Merkle trees with a `network_fee` through the registry program. These fee-based trees are serviced by Light Protocol's forester network. The user wants to restrict creation of fee-based trees to only the protocol authority (`protocol_config_pda.authority`), while keeping non-forested V1 trees (no network_fee, designated forester) open for anyone.

## Scope

**Instructions affected (in `programs/registry/src/lib.rs`):**

1. **`initialize_batched_state_merkle_tree`** (line 499): Always requires `network_fee` -> add authority check
2. **`initialize_batched_address_merkle_tree`** (line 582): Always requires `network_fee` -> add authority check
3. **`initialize_state_merkle_tree`** (line 325): `network_fee` is optional -> add authority check only when `network_fee.is_some()`

**Instructions NOT affected:**

- `initialize_address_merkle_tree` (line 284): Already forces `network_fee = None`, no change needed
- Rollover instructions: Operational maintenance by foresters, not new user-facing tree creation

## Tasks

### Task 1: Add authority check in `initialize_batched_state_merkle_tree`

**File:** `programs/registry/src/lib.rs` (line ~510)

After the existing `network_fee` validation, add:
```rust
if ctx.accounts.authority.key() != ctx.accounts.protocol_config_pda.authority {
    return err!(RegistryError::InvalidSigner);
}
```

Reuse the existing `InvalidSigner` error variant.

### Task 2: Add authority check in `initialize_batched_address_merkle_tree`

**File:** `programs/registry/src/lib.rs` (line ~593)

Same pattern as Task 1:
```rust
if ctx.accounts.authority.key() != ctx.accounts.protocol_config_pda.authority {
    return err!(RegistryError::InvalidSigner);
}
```

### Task 3: Add conditional authority check in `initialize_state_merkle_tree`

**File:** `programs/registry/src/lib.rs` (line ~340)

Inside the `if let Some(network_fee) = merkle_tree_config.network_fee` block, after validating the fee value and before the forester check:
```rust
if ctx.accounts.authority.key() != ctx.accounts.protocol_config_pda.authority {
    return err!(RegistryError::InvalidSigner);
}
```

The `else` branch (no network_fee, with designated forester) remains unchanged -- anyone can create V1 non-forested trees.

### Task 4: Update test helpers to use protocol authority

**File:** `sdk-libs/program-test/src/accounts/state_tree_v2.rs` (`create_batched_state_merkle_tree`)
**File:** `sdk-libs/program-test/src/accounts/address_tree_v2.rs` (`create_batch_address_merkle_tree`)
**File:** `sdk-libs/program-test/src/accounts/state_tree.rs` (`create_initialize_merkle_tree_instruction`) -- only for fee-based path

These helpers currently use the `payer` as authority. We need to pass the protocol authority as the signer for fee-based trees. This may require:
- Adding an `authority` parameter to the helper functions, or
- Passing the protocol authority keypair into these functions

### Task 5: Run tests and fix any breakage

```bash
cargo test-sbf -p account-compression-test
cargo test-sbf -p system-test
```

## Verification

1. Batched state/address tree creation fails with `InvalidSigner` when authority is not protocol authority
2. V1 state tree creation with network_fee fails with `InvalidSigner` when authority is not protocol authority
3. V1 state tree creation without network_fee (with forester) succeeds with any authority
4. V1 address tree creation continues to work unchanged
5. Rollover instructions continue to work unchanged
6. All existing tests pass after helper updates


If you need specific details from before exiting plan mode (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/ananas/.REDACTED.jsonl

---

You are a senior code reviewer ensuring high standards of code quality and security.

When invoked:
1. Take a step back, think hard and be critical.
2. Run `git diff` and `git diff --cached` to see all changes (unstaged and staged) unless instructed otherwise
3. Focus on modified files
4. Create state machine diagrams (internally) to understand the flow:
   - Identify entry points and exit points
   - Map state transitions and decision branches
   - Trace data flow through functions
   - For complex reviews, create multiple diagrams:
     - One high-level diagram showing overall architecture and module interactions
     - Multiple lower-level diagrams for each significant component/function
   - Write diagrams to /tmp/review-diagrams/ for reference
5. Begin review with full understanding of the code flow


### 1. Correctness

Review the plan and conversation history - is the implementation actually done and does it do what was requested?
Does it follow solana best practices see ~/dev/claude-context/CLAUDE.md, specifically solana tips and check whether you can get additional context to consider in your review?
Does duplicate code exist?

### 2. Test Assertions

When reviewing tests that check account state:
- Prefer borsh deserialization with single `assert_eq` against expected reference account
- Flag magic byte offset assertions like `account.data[108]` - suggest struct deserialization instead
- Flag multiple scattered assertions on individual fields - suggest single comparison against expected struct

```rust
// Deserialize the account
let account = AccountType::deserialize(&mut &account.data[..])?;

// Build expected account for comparison
let expected = AccountType {
    field1: value1,
    field2: value2,
    // ... all fields
};

// Single assert comparing full account state
assert_eq!(account, expected, "Account should match expected");
```

### 3. Dead Code

Flag dead code patterns:
- Underscore-prefixed variables that suppress warnings: `let _owner_program_id = ...` - remove unused variables entirely
- Do not use underscore prefix to silence warnings - delete the dead code instead
- Do not add `#[allow(dead_code)]` or `#[allow(unused_variables)]` - remove the dead code instead

### 4. Backwards Compatibility Hacks

Flag and warn on backwards compatibility patterns - these add unnecessary complexity:
- Re-exporting types/functions that are no longer used internally
- Adding `// removed`, `// deprecated`, or `// legacy` comments for removed code
- Renaming unused parameters to `_param` instead of removing them from the signature
- Feature flags added solely for backwards compatibility (e.g., `#[cfg(feature = "legacy")]`)
- Wrapper functions that just delegate to new implementations
- `#[deprecated]` attributes on new code - if it's deprecated, don't add it
- Keeping old enum variants that are never constructed
- Shim modules that re-export from new locations
- Default trait implementations that exist only for backwards compatibility
- `From`/`Into` implementations between old and new types when old type should be deleted
- Version-specific conditional compilation for old behavior

Prefer clean breaks:
- Delete unused code entirely rather than commenting or deprecating
- Remove unused function parameters from signatures
- Delete old enum variants when they're no longer valid
- Remove re-exports when the canonical path changes
- If something is unused, it should not exist in the codebase

---

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Analysis:
Let me carefully analyze the entire conversation chronologically:

1. The user provided a plan to implement "Gate Network-Fee Tree Creation to Protocol Authority" with 5 tasks.

2. I read the relevant files:
   - `programs/registry/src/lib.rs` - the main registry program
   - `programs/registry/src/errors.rs` - error definitions
   - `sdk-libs/program-test/src/accounts/state_tree_v2.rs` - test helper for batched state trees
   - `sdk-libs/program-test/src/accounts/address_tree_v2.rs` - test helper for batched address trees
   - `sdk-libs/program-test/src/accounts/state_tree.rs` - test helper for V1 state trees
   - `programs/registry/src/account_compression_cpi/sdk.rs` - SDK instruction builders

3. Tasks 1-3: Added authority checks in lib.rs:
   - Task 1: `initialize_batched_state_merkle_tree` - added check AFTER the if/else network_fee block
   - Task 2: `initialize_batched_address_merkle_tree` - same pattern
   - Task 3: `initialize_state_merkle_tree` - added check INSIDE the `if let Some(network_fee)` block

4. Task 4: Updated test helpers and SDK builders:
   - Renamed `payer` to `authority` in 3 SDK instruction builders
   - Added `authority: &Keypair` parameter to 3 test helper functions
   - Updated all callers (~13 files)

5. Task 5: Ran tests:
   - `account-compression-test`: All passed (24 passed, 2 ignored)
   - `registry-test --test tests`: Most passed, `test_register_and_update_forester_pda` had stack overflow (pre-existing, passes with RUST_MIN_STACK=8388608)
   - `registry-test --test test_register_anytime`: `test_forester_register_outside_registration_phase` failed (pre-existing, unrelated to changes)
   - `system-test`: `invoke_failing_test` failed (pre-existing, unrelated to changes)

6. The user then invoked `/review-diff` which triggered a code review skill. The review was in plan mode and I was reading the diff and analyzing it when the summary was requested.

Key findings from the review analysis I was doing:
- The on-chain authority checks are correctly placed
- The placement differs between V1 (inside if-let) and batched (outside if-else) but both are correct due to control flow
- Missing negative tests for the new InvalidSigner error
- The registry test uses `&updated_keypair` for the success case with network_fee, which is correct since updated_keypair is the protocol authority
- xtask callers use `&payer` for both, which works when payer IS the protocol authority

The user's messages were:
1. "Implement the following plan: [detailed plan]"
2. (Exited plan mode - system message)
3. "/review-diff" - triggered code review
4. Now requesting summary

Let me note all the specific files modified and their changes.

Summary:
1. Primary Request and Intent:
   The user requested implementation of a plan titled "Gate Network-Fee Tree Creation to Protocol Authority." The goal is to restrict creation of Merkle trees with `network_fee` (fee-based trees serviced by Light Protocol's forester network) to only the protocol authority (`protocol_config_pda.authority`). Non-forested V1 trees (no network_fee, with designated forester) remain open for anyone to create. After implementation was complete, the user invoked `/review-diff` to perform a code review of the changes.

2. Key Technical Concepts:
   - Light Protocol ZK Compression on Solana
   - Registry program wrapping Account Compression with access control
   - Batched Merkle trees (V2) vs concurrent Merkle trees (V1)
   - Network fee-based trees serviced by Light foresters vs designated forester trees
   - `protocol_config_pda.authority` - the protocol authority that controls fee-based tree creation
   - `RegistryError::InvalidSigner` - reused existing error variant for authority check failures
   - Anchor framework account contexts with `Signer<'info>` for authority
   - CPI pattern: registry program delegates to account compression program via PDA signer

3. Files and Code Sections:

   - **`programs/registry/src/lib.rs`** - Core on-chain program with 3 authority checks added
     - Task 1: `initialize_batched_state_merkle_tree` (~line 524) - authority check placed AFTER the if/else network_fee block (else always returns error, so check only runs for valid fee):
       ```rust
       if ctx.accounts.authority.key() != ctx.accounts.protocol_config_pda.authority {
           return err!(RegistryError::InvalidSigner);
       }
       ```
     - Task 2: `initialize_batched_address_merkle_tree` (~line 615) - same pattern as Task 1
     - Task 3: `initialize_state_merkle_tree` (~line 348) - authority check placed INSIDE the `if let Some(network_fee)` block, after forester check:
       ```rust
       if ctx.accounts.authority.key() != ctx.accounts.protocol_config_pda.authority {
           return err!(RegistryError::InvalidSigner);
       }
       ```
     - The else branch (no network_fee with designated forester) remains unchanged

   - **`programs/registry/src/errors.rs`** - Read only, confirmed `InvalidSigner` variant exists at line 26

   - **`programs/registry/src/account_compression_cpi/sdk.rs`** - SDK instruction builders renamed `payer` param to `authority` in 3 functions:
     - `create_initialize_batched_merkle_tree_instruction(authority: Pubkey, ...)` 
     - `create_initialize_batched_address_merkle_tree_instruction(authority: Pubkey, ...)`
     - `create_initialize_merkle_tree_instruction(authority: Pubkey, ...)`
     - All changed `authority: payer` to `authority` in the accounts struct

   - **`sdk-libs/program-test/src/accounts/state_tree_v2.rs`** - Added `authority: &Keypair` param after `payer`:
     ```rust
     pub async fn create_batched_state_merkle_tree<R: Rpc>(
         payer: &Keypair,
         authority: &Keypair,
         registry: bool,
         ...
     ```
     - Uses `authority.pubkey()` for registry instruction builder
     - Conditionally adds authority to signers: `if registry && authority.pubkey() != payer.pubkey() { signers.push(authority); }`

   - **`sdk-libs/program-test/src/accounts/address_tree_v2.rs`** - Added `authority: &Keypair` param after `payer`:
     ```rust
     pub async fn create_batch_address_merkle_tree<R: Rpc>(
         rpc: &mut R,
         payer: &Keypair,
         authority: &Keypair,
         ...
     ```
     - Same signer pattern as state_tree_v2

   - **`sdk-libs/program-test/src/accounts/state_tree.rs`** - Added `authority: &Keypair` param after `payer`:
     ```rust
     pub async fn create_state_merkle_tree_and_queue_account<R: Rpc>(
         payer: &Keypair,
         authority: &Keypair,
         registry: bool,
         ...
     ```
     - Uses `authority.pubkey()` in registry path instruction builder
     - Conditionally adds authority to signers when different from payer

   - **`sdk-libs/program-test/src/accounts/initialize.rs`** - Updated 4 calls to pass `&keypairs.governance_authority` for both payer and authority

   - **`sdk-libs/program-test/src/indexer/test_indexer.rs`** - Updated 3 calls to pass `&self.payer` for both payer and authority

   - **`program-tests/registry-test/tests/tests.rs`** - Updated 6 calls:
     - V1 tree with `network_fee: None` + forester -> `&payer` as authority (no check triggered)
     - V1 tree with `network_fee: None` + no forester -> `&payer` (fails before authority check: ForesterUndefined)
     - V1 tree with `network_fee: Some(5000)` (success case) -> `&updated_keypair` as authority (this IS the protocol authority)
     - V1 tree with `network_fee: Some(5000)` + forester -> `&payer` (fails before authority check: ForesterDefined)
     - V1 tree with `network_fee: Some(5001)` -> `&payer` (fails before authority check: InvalidNetworkFee)
     - Batched address tree with `network_fee: Some(1)` -> `&payer` (fails before authority check: InvalidNetworkFee)

   - **`program-tests/account-compression-test/tests/batched_merkle_tree_test.rs`** - Updated 1 call, `registry: false` so authority doesn't matter

   - **`program-tests/utils/src/e2e_test_env.rs`** - Updated 1 call, passes `&self.payer` for both

   - **`xtask/src/create_state_tree.rs`** - Updated 1 call, passes `&payer` for both
   - **`xtask/src/create_batch_state_tree.rs`** - Updated 1 call, passes `&payer` for both
   - **`xtask/src/create_batch_address_tree.rs`** - Updated 1 call, passes `&payer` for both
   - **`xtask/src/new_deployment.rs`** - Updated 2 calls, passes `&test_keypairs.governance_authority` for both

   - **`programs/registry/src/account_compression_cpi/initialize_batched_state_tree.rs`** - Read only, confirmed `authority: Signer<'info>` with `#[account(mut)]`

   - **`program-tests/system-cpi-test/tests/test_program_owned_trees.rs`** - NOT modified, has its own local `create_state_merkle_tree_and_queue_account` with different signature

4. Errors and fixes:
   - **Compilation errors after adding authority parameter**: Each test helper signature change caused compilation errors in all callers. Fixed by systematically finding and updating all call sites using an Explore subagent, then editing each caller.
   - **`test_register_and_update_forester_pda` stack overflow**: Pre-existing issue. The test function is 1300+ lines, causing stack overflow with default thread stack size. Passes with `RUST_MIN_STACK=8388608`. Not fixed - reverted attempted annotation change `#[tokio::test(flavor = "multi_thread", worker_threads = 1)]` back to `#[tokio::test]`.
   - **`test_forester_register_outside_registration_phase` failure**: Pre-existing, unrelated to changes. Error: `NotInRegistrationPeriod` in forester epoch registration.
   - **`invoke_failing_test` failure in system-test**: Pre-existing, unrelated. About system program error validation.

5. Problem Solving:
   - Successfully identified that the authority check placement differs between V1 (inside if-let block because else doesn't always error) and batched (outside if-else block because else always returns error) - both are correct.
   - Identified `updated_keypair` as the protocol authority in registry tests (set via `UpdateProtocolConfig` earlier in the test), not `payer`.
   - Confirmed `system-cpi-test` has its own local function and doesn't need updating.
   - All `cargo check` passes for all relevant crates.
   - The `/review-diff` was just started - the diff was read and analysis was beginning.

6. All user messages:
   - "Implement the following plan: [detailed 5-task plan for gating network-fee tree creation to protocol authority]" with reference to a plan file transcript
   - (System message: Exited Plan Mode)
   - "/review-diff" - invoked code review skill

7. Pending Tasks:
   - The `/review-diff` code review was in progress when summary was requested. The review skill instructions specify checking for: correctness, test assertions, dead code, backwards compatibility hacks. The diff was read and analysis was partially complete.
   - Key review findings identified but not yet written up:
     - Missing negative tests for the new `InvalidSigner` error (no test verifies that a non-protocol-authority gets rejected when creating fee-based trees)
     - The authority check placement is correct but inconsistent between V1 and batched (for valid reasons)
     - No backwards compatibility hacks or dead code issues found

8. Current Work:
   The `/review-diff` code review skill was invoked. I had read the full `git diff` output, read the key modified sections of `programs/registry/src/lib.rs` at lines 337-361, 505-539, and 596-619, and read the registry test changes at lines 384-509. I was in plan mode analyzing the diff for correctness, test assertions, dead code, and backwards compatibility hacks. The review was interrupted by the summary request before findings could be written to the plan file and presented to the user.

9. Optional Next Step:
   Complete the `/review-diff` code review. The user explicitly invoked this skill. Key finding to report: **Missing negative test for `InvalidSigner`** - there is no test that verifies a non-protocol-authority user gets `InvalidSigner` when attempting to create a fee-based V1 state tree, batched state tree, or batched address tree. The existing tests only cover the success case (correct authority) and failure cases that trigger errors before the authority check.

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/ananas/.REDACTED.jsonl
Please continue the conversation from where we left off without asking the user any further questions. Continue with the last task that you were asked to work on.

---

[Request interrupted by user for tool use]

---

what about address tree v1 is it checked?

---

ok fix issue 1