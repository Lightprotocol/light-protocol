{
  "name": "@lightprotocol/memo-example",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "Memo example",
  "scripts": {
    "test": "cargo test-sbf -p memo -- --test-threads 1"
  },
  "nx": {
    "targets": {
      "build": {
        "outputs": [
          "{workspaceRoot}/target/deploy"
        ]
      }
    }
  },
  "devDependencies": {
    "@lightprotocol/zk-compression-cli": "workspace:*"
  }
}
