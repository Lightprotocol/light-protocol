[package]
name = "light-compressed-token"
version = "0.4.0"
description = "Generalized token compression on Solana"
repository = "https://github.com/Lightprotocol/light-protocol"
license = "Apache-2.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "light_compressed_token"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
custom-heap = ["light-heap"]
mem-profiling = []
default = ["custom-heap"]
test-sbf = []
bench-sbf = []

[dependencies]
anchor-lang = "0.29.0"
anchor-spl = "0.29.0"
aligned-sized = { version = "0.1.1", path = "../../macros/aligned-sized" }
account-compression = { version = "0.3.2", path = "../account-compression", features = ["cpi"] }
light-system-program = { version = "0.3.1", path = "../system", features = ["cpi"] }
bytemuck = "1.14"
solana-security-txt = "1.1.0"
light-hasher = { version = "0.1.1", path = "../../merkle-tree/hasher" }
light-heap = { version = "0.1.1", path = "../../heap", optional = true }
light-utils = { version = "0.1.1", path = "../../utils" }
light-verifier = { version = "0.1.1", path = "../../circuit-lib/verifier" }

spl-token = "3.5.0"

[target.'cfg(not(target_os = "solana"))'.dependencies]
solana-sdk = "1.18.11"

[dev-dependencies]
solana-program-test = "1.18.11"
reqwest = "0.11.26"
tokio = "1.36.0"
light-circuitlib-rs = { version = "0.1.1", path = "../../circuit-lib/circuitlib-rs" }
num-bigint = "0.4.4"
num-traits = "0.2.18"
light-merkle-tree-reference = { version = "0.1.1", path = "../../merkle-tree/reference" }
light-concurrent-merkle-tree = { version = "0.1.1", path = "../../merkle-tree/concurrent" }
rand = "0.8.5"
