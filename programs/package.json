{
  "name": "@lightprotocol/programs",
  "version": "0.3.0",
  "license": "GPL-3.0",
  "scripts": {
    "build": "anchor build",
    "test": "cargo test-sbf -p account-compression && cargo test-sbf -p psp-compressed-pda && cargo test-sbf -p psp-compressed-token"
  }
}
