I will add a print to programs that v1 trees are deprecated, and a link to the migration guide https://www.zkcompression.com/resources/migration-v1-to-v2

---

ok next review how we charge fees in the system program

---

we should set it for v1 tress the same way as for v2 trees   │ Output        │ reads from legacy_merkle_context (no explicit network fee call, but           │ set_network_fee_v2() -- charges once per tree (same flag, so if already set by inputs,  │
  │ (append)      │ set_rollover_fee is called)                                                   │ no additional charge)

---

plan out B

---

[Request interrupted by user for tool use]

---

Use a subagent with model=opus to validate the current plan.

The subagent should analyze the plan and answer these questions:

1. Are there any open questions?
2. Are there any conflicting objectives?

Report findings clearly and suggest resolutions if issues are found.


ARGUMENTS: keep diff minimal

---

[Request interrupted by user for tool use]