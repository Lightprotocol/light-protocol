//! Seed derivation - dead code (not called from lib.rs).
//!
//! The old `derive_seed` function is no longer used.
//! Seed derivation for variants is handled by `accounts/variant.rs`.
//! Seed derivation for decompression is handled by `program/decompress.rs`.
