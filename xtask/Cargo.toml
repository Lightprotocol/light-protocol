[package]
name = "xtask"
version = "0.1.0"
edition = "2021"


[dependencies]
anyhow = "1.0"
clap = { version = "4", features = ["derive"] }
light-merkle-tree = { path = "../merkle-tree/sparse", version = "0.1.1" }
light-utils = { path = "../utils", version = "0.1.0" }
quote = "1.0"
sha2 = "0.10"
