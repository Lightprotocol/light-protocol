// this should maybe be empty and just render the modal container for shielding
// this should have it's own wallet connector context.
// we ignore wallet ctx for now. and just assume a Wallet (by keypair). let's also directly extract all account logic/wallet into its own ctx
// several ideas for then dealing with wallet connection.

// once we have a seed in the web wallet, we dont need wallet connection. (except for shield flow);
