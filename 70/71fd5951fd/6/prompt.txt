Implement the following plan:

# Plan: Add network fee on V1 output appends

## Context

V1 state tree outputs (appends) do not charge a network fee. V2 outputs charge 5,000 lamports (once per tree) via `set_network_fee_v2()`. This is inconsistent -- V1 outputs should also charge the network fee. Since V1 is deprecated, this is a minor fee increase for remaining V1 users, encouraging migration.

## Changes

### 1. Add `set_network_fee_v1` call in V1 output path

**File:** `programs/system/src/processor/create_outputs_cpi_data.rs`

In the `AcpAccount::StateTree` branch (line ~93-109), after extracting `merkle_context`, add:

```rust
context.set_network_fee_v1(merkle_context.network_fee, current_index as u8)?;
```

This uses the existing additive `set_network_fee_v1` method, consistent with V1 input fee charging. The fee goes to the state tree account (same destination as the rollover fee for that tree).

Note: `set_network_fee_v1` is additive per call. For outputs, it's called once per unique output tree (since the branch only executes when `current_index` changes). So a tx with outputs to 1 V1 tree = +5,000; outputs to 2 V1 trees = +10,000.

### 2. Update `assert_transaction_params` fee calculation

**File:** `sdk-libs/client/src/fee.rs`

Add a new field to `TransactionParams`:
```rust
pub v1_output_compressed_accounts: u8,
```

Update the network fee calculation (~line 73) to also account for V1 output trees. Keep the existing `else if` between V1 and V2 input checks:
```rust
// Input network fee (V1 per-input-tree, V2 once-per-tree, mutually exclusive)
if transaction_params.v1_input_compressed_accounts != 0 {
    network_fee += transaction_params.fee_config.network_fee as i64
        * transaction_params.v1_input_compressed_accounts as i64;
} else if transaction_params.v2_input_compressed_accounts {
    network_fee += transaction_params.fee_config.network_fee as i64;
}
// Output network fee (V1 only, per-output-tree)
if transaction_params.v1_output_compressed_accounts != 0 {
    network_fee += transaction_params.fee_config.network_fee as i64
        * transaction_params.v1_output_compressed_accounts as i64;
}
```

Note: V1 output fee is per-tree (not per-output-account), so this field should represent the number of unique V1 output trees.

### 3. Update doc comment in context.rs

**File:** `programs/system/src/context.rs` (lines 161-176)

Update the V1 examples to reflect the new output fee:
```
/// - V1 state trees: charge per input tree + per output tree + per address
/// ...
/// Examples (V1 state trees):
/// 1. create account (0 inputs, 1 output tree, 1 address):     network fee 15,000 lamports (0x5k input + 1x5k output + 1x10k address)
/// 2. token transfer (1 input, 1 output, same tree):           network fee 10,000 lamports (1x5k input + 1x5k output)
/// 3. transfer (2 inputs, 1 output tree, 1 address):           network fee 25,000 lamports (2x5k input + 1x5k output + 1x10k address)
```

### 4. Update all `TransactionParams` construction sites

Add `v1_output_compressed_accounts` field to every existing `TransactionParams` construction. These all need the new field:

- `program-tests/system-test/tests/test.rs` (~4 sites)
- `program-tests/utils/src/e2e_test_env.rs` (~15 sites)
- `program-tests/system-cpi-test/tests/test_program_owned_trees.rs`

For V1 tests: set to the number of unique output trees (usually 1).
For V2/batched tests: set to 0.

### 5. Fix JS test fee assertions (DONE: steps 1-4, REMAINING: step 5)

Two JS test files have balance assertions that are now off by 5000 lamports (the new V1 output network fee).

#### 5a. `js/stateless.js/tests/e2e/compress.test.ts` (lines 46-53)

The `txFees()` helper computes `networkInFee` which handles V1 input fees and V2 once-per-tree fees. It does NOT account for V1 output fees. Add a `networkOutFee` term for V1 outputs.

Current (line 46-53):
```typescript
const networkInFee = tx.in
    ? featureFlags.isV2()
        ? STATE_MERKLE_TREE_NETWORK_FEE
        : STATE_MERKLE_TREE_NETWORK_FEE.mul(bn(tx.in))
    : tx.out && featureFlags.isV2()
      ? STATE_MERKLE_TREE_NETWORK_FEE
      : bn(0);
```

Add after `networkInFee` (line 54):
```typescript
/// V1 output network fee: charge once per output tree (tests use 1 tree)
const networkOutFee = tx.out && !featureFlags.isV2()
    ? STATE_MERKLE_TREE_NETWORK_FEE
    : bn(0);
```

Update the total (line 65-70) to include `.add(networkOutFee)`:
```typescript
totalFee = totalFee.add(
    solanaBaseFee
        .add(stateOutFee)
        .add(addrFee)
        .add(networkInFee)
        .add(networkOutFee)
        .add(networkAddressFee),
);
```

#### 5b. `js/stateless.js/tests/e2e/test-rpc.test.ts`

**Line 64-66** -- compress operation (0 inputs, 1 output). V1 now charges output fee:
```typescript
// Old: featureFlags.isV2() ? STATE_MERKLE_TREE_NETWORK_FEE.toNumber() : 0
// New: both V1 and V2 charge the network fee for outputs
let expectedFee = STATE_MERKLE_TREE_NETWORK_FEE.toNumber();
```

**Lines 109-114** -- transfer operation (1 input, 1 output). V1 now charges for both input AND output tree:
```typescript
// Old: 1x NETWORK_FEE
// New: V1 = 2x (input + output), V2 = 1x (once-per-tree)
assert.equal(
    postCompressBalance,
    preCompressBalance -
        5000 -
        STATE_MERKLE_TREE_ROLLOVER_FEE.toNumber() -
        STATE_MERKLE_TREE_NETWORK_FEE.toNumber() *
            (featureFlags.isV2() ? 1 : 2),
);
```

## Verification

1. `cargo check -p light-system-program-pinocchio` -- confirms on-chain code compiles (DONE)
2. `cargo check -p light-client` -- confirms fee assertion compiles (DONE)
3. `cargo test-sbf -p system-test` -- integration tests with fee assertions pass (DONE)
4. `cargo test-sbf -p system-cpi-test` -- CPI tests pass (DONE)
5. `just js::test-stateless` -- JS stateless.js tests pass (V1 and V2)


If you need specific details from before exiting plan mode (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/ananas/.REDACTED.jsonl

---

you shoul run the tests